# Lo lee docker-compose.yml (raíz del repo). Copiar como .env y cambiar el valor.
# Mínimo 8 caracteres con mayúscula, minúscula, número y símbolo; evitar @ : / ? #
MSSQL_SA_PASSWORD=Cambia-Esta-Clave-123
