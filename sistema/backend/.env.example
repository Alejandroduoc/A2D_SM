# Lo carga docker-compose.yml dentro del contenedor del backend. Copiar como .env.
# DATABASE_URL NO va aquí: el docker-compose.yml la arma solo (host "db", driver 18)
# usando MSSQL_SA_PASSWORD de sistema/.env.
SECRET_KEY=cambia-esta-clave-larga-y-aleatoria
